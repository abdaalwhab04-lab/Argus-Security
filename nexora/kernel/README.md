# NEXORA Linux Kernel

This directory contains the Linux Kernel integration for NEXORA.

Target architecture:

- x86_64

Kernel configuration:

- `../config/kernel.conf`

The kernel will be downloaded and built by the NEXORA GitHub Actions build workflow.

The local UserLAnd environment is used for preparation and validation, not for the final kernel build.
