# NEXORA Linux Root Filesystem

This directory defines the root filesystem layout for NEXORA Linux.

The final root filesystem will contain:

- Essential system directories
- NEXORA configuration
- Runtime directories
- Argus integration
- Required userspace tools

The complete filesystem will be generated during the NEXORA build.
